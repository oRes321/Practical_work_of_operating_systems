

/*fn main() {
    let (x, y, z);

    // Fill the blank
    let (y,z,x): (i32,i32,i32,) = (1, 2, 3);

    assert_eq!(x, 3);
    assert_eq!(y, 1);
    assert_eq!(z, 2);

    println!("Success!");
}*/