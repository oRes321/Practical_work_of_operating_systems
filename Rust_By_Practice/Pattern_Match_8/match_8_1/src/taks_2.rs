#[test]

fn main() {
    let boolean = true;

    // Fill the blank with a match expression:
    //
    // boolean = true => binary = 1
    // boolean = false =>  binary = 0
    let binary =  true;

    assert_eq!(binary, boolean);

    println!("Success!");
}